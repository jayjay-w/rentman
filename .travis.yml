before_install:
 - uname -a
 - sudo add-apt-repository --yes ppa:ubuntu-sdk-team/ppa
 - sudo apt-get install -qq cmake build-essential qtbase5-dev libsqlite3-dev qt5-default qttools5-dev-tools
before_script:
 - mkdir build
 - cd build
 - cmake ..

script:
 - make clean
 - make

notifications:
 recepients:
  - joejoshw@gmail.com
  - joshua@megvel.me.ke
 email:
  on_success: always
  on_failure: always
 irc:
  channels:
   - "irc.freenode.net#freepayrollke"
  on_success: always
  on_failure: always
